apiVersion: v1
kind: Service
metadata:
  annotations:
    service.alpha.kubernetes.io/tolerate-unready-endpoints: "true"
  name: kafka
  labels:
    service: kafka
spec:
  ports:
    - name: broker
      port: 9092
  clusterIP: None
  selector:
    app: kafka
